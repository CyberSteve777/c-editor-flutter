# Documentary package metadata — mirrors plugin_example/hello_cplugin layout.
# Embedded under package:c_editor (lib/bundled_plugins/...) for in-process load.
name: dynamic_fetch_cplugin
description: Bundled C-Editor plugin — fetch pvz2c dynamic.rsb.smf for RSB export
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
