# Removed

Upstream flutter_eval example `integration_test` files were removed from this
vendored copy. They targeted a counter-app template that does not match
`example/lib/main.dart` and are not used by C-Editor.
