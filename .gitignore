# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.dart-tool/
.dartServer/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/
.artifact/
# FVM local SDK symlink/cache (keep .fvmrc committed)
.fvm/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Generated RSB export Web Worker build noise. The compiled
# `web/rsb_worker.dart.js` itself is committed so `flutter build web` works out
# of the box; recompile it with tool/build_web when the sen pipeline changes.
web/rsb_worker.dart.js.deps
web/rsb_worker.dart.js.map

# Other
/z-editor-original
/ios-builder.exe
/dist
/.mobai

#/assets/
report.txt
/ANDROID_LASTEST_BUILD
/android/signing
android/app/release-keystore.jks
local.properties
/.vscode/launch.json
/.devcontainer
run.sh
dart_defines.json
